{
    "name": "@truecost/shared",
    "version": "0.1.0",
    "main": "dist/index.js",
    "files": [
        "dist/"
    ],
    "scripts": {
        "clean": "yarn build:clean",
        "watch": "yarn build:watch",
        "build": "yarn build:clean && tsc",
        "build:clean": "rimraf dist tsconfig.tsbuildinfo",
        "build:watch": "yarn build --watch"
    },
    "private": true,
    "license": "MIT"
}
