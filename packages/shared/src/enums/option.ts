export enum OptionType {
    NOMINAL = 'nominal',    //  $
    RELATIVE = 'relative',  //  %
}

export enum OptionFilter {
    EXCLUDE = 'exclude',    //  exclude specified items (all but)
    INCLUDE = 'include',    //  include specified items (only)
}

export enum OptionArea {
    GLOBAL = 'global',      //  applied to order
    LOCAL = 'local',        //  applied to item
}
